services:
  server:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      DATA_DIR: /data
    volumes:
      - pilot_data:/data
    restart: unless-stopped

volumes:
  pilot_data:
