{
  "name": "pilotapp",
  "private": true,
  "scripts": {
    "dev":        "concurrently -n server,web -c cyan,magenta \"cd server && npm run dev\" \"cd web && npm run dev\"",
    "dev:server": "cd server && npm run dev",
    "dev:web":    "cd web && npm run dev",
    "build":      "cd web && npm run build && cd ../server && npm run build",
    "start":      "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
