{
  "name": "pilotapp",
  "private": true,
  "workspaces": ["packages/*", "web", "electron", "electron/renderer"],
  "scripts": {
    "tokens":     "node design/build.mjs",
    "dev":        "npm run tokens && concurrently -n server,web -c cyan,magenta \"cd server && npm run dev\" \"cd web && npm run dev\"",
    "dev:server": "cd server && npm run dev",
    "dev:web":    "cd web && npm run dev",
    "build":      "npm run tokens && cd web && npm run build && cd ../server && npm run build",
    "start":      "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
