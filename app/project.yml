name: Pilot
options:
  bundleIdPrefix: com.pilotapp
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16"

settings:
  base:
    SWIFT_VERSION: "6.0"
    ENABLE_PREVIEWS: YES

targets:
  Pilot:
    type: application
    platform: iOS
    sources:
      - Pilot
    resources:
      - Pilot/Sprites
    info:
      path: Pilot/Info.plist
      properties:
        CFBundleDisplayName: Pilot
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        NSMicrophoneUsageDescription: Used for voice input to agent tasks
        NSSpeechRecognitionUsageDescription: Used to transcribe voice commands into prompts
        UILaunchScreen:
          UIColorName: ""
          UIImageName: ""
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - pilotapp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.pilotapp.pilot
        DEVELOPMENT_TEAM: ""
